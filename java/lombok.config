# do not search parent directories for Lombok configuration
config.stopBubbling = true

# add @lombok.Generated annotation to generated methods so Jacoco will ignore them
lombok.addLombokGeneratedAnnotation = true
